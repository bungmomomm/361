import axios from 'axios';
import cookie from 'react-cookie';
import { 
	ADDR_GET_ADDRESS,
	ADDR_SAVE_ADDRESS,
 ADDR_DROP_SHIPPER,
 ADDR_O2O_LIST 
} from './constants';


const getAddress = (userId, email) => {
 return () => {
  
 };
};
